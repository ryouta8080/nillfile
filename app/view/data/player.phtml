<?php
$embedUrl = $this->view->embed;
$videoUrl = $this->view->video;
$downloadUrl = $this->view->download;
?>
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Video</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <link rel="alternate" type="application/json+oembed" href="<?php $this->h->p($embedUrl); ?>" />
  <style>
    body {
      margin: 0;
      background: #000;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      flex-direction: column;
    }
    video {
      width: 100%;
      height: auto;
      max-height: 90vh;
      background: #000;
    }
    .download-btn {
      margin-top: 10px;
      background: rgba(255,255,255,0.9);
      color: #000;
      padding: 10px 20px;
      border: none;
      border-radius: 6px;
      text-decoration: none;
      font-size: 14px;
      transition: 0.2s;
    }
    .download-btn:hover {
      background: #fff;
      color: #000;
      text-decoration: none;
      box-shadow: 0 2px 5px rgba(255,255,255,0.3);
    }
  </style>
</head>
<body>
  <video controls playsinline loop>
    <source src="<?php $this->h->p($videoUrl); ?>" type="video/mp4">
    お使いのブラウザは video タグをサポートしていません。
  </video>

  <?php if (!empty($downloadUrl)): ?>
    <a class="download-btn" href="<?php $this->h->p($downloadUrl); ?>" download>
      <i class="bi bi-download"></i> ダウンロード
    </a>
  <?php endif; ?>
</body>
</html>
<?php
$downloadFileName = (string)($this->view->downloadFileName ?? '');
$downloadUrl = (string)($this->view->downloadUrl ?? '');
if (!function_exists('content_zip_h')) {
    function content_zip_h($value): string {
        return htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
    }
}
?>
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZIPダウンロード</title>
  <style>
  * { box-sizing: border-box; }
  body {
    background: #f5f6f7;
    color: #24292f;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    margin: 0;
    min-height: 100vh;
  }
  main {
    margin: 0 auto;
    max-width: 720px;
    padding: 12vh 20px 48px;
  }
  .download-icon {
    align-items: center;
    background: #e7f1ff;
    border: 1px solid #b6d4fe;
    border-radius: 6px;
    color: #0d6efd;
    display: flex;
    font-size: 28px;
    height: 56px;
    justify-content: center;
    margin-bottom: 20px;
    width: 56px;
  }
  h1 {
    font-size: 24px;
    margin: 0 0 10px;
  }
  .file-name {
    background: #fff;
    border: 1px solid #d0d7de;
    border-radius: 4px;
    color: #24292f;
    font-size: 14px;
    margin: 0 0 20px;
    overflow-wrap: anywhere;
    padding: 10px 12px;
  }
  .download-help {
    color: #57606a;
    font-size: 14px;
    line-height: 1.7;
    margin-bottom: 24px;
  }
  .download-button {
    align-items: center;
    background: #0d6efd;
    border: 1px solid #0d6efd;
    border-radius: 4px;
    color: #fff;
    display: inline-flex;
    font-size: 15px;
    justify-content: center;
    min-height: 44px;
    padding: 10px 18px;
    text-decoration: none;
  }
  .download-button:focus,
  .download-button:hover {
    background: #0b5ed7;
    border-color: #0a58ca;
  }
  @media (max-width: 575px) {
    main { padding-top: 64px; }
    h1 { font-size: 21px; }
    .download-button { width: 100%; }
  }
  </style>
</head>
<body>
<main>
  <div class="download-icon" aria-hidden="true">ZIP</div>
  <h1>ZIPダウンロードを開始しました</h1>
  <?php if ($downloadFileName !== ''): ?>
    <div class="file-name"><?php echo content_zip_h($downloadFileName); ?></div>
  <?php endif; ?>
  <p class="download-help">しばらく待ってもダウンロードが開始しない場合は、以下のリンクを押してください。</p>
  <a class="download-button" id="downloadButton" href="<?php echo content_zip_h($downloadUrl); ?>">ZIPをダウンロード</a>
</main>
<script>
(function(){
  var downloadUrl = <?php echo json_encode($downloadUrl, JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_QUOT | JSON_HEX_AMP); ?>;
  if (!downloadUrl) return;
  window.setTimeout(function(){
    window.location.assign(downloadUrl);
  }, 400);
})();
</script>
</body>
</html>
